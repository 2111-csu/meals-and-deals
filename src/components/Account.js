import React from "react";
const Account = ({userId}) => {
   
    return <>
        <div>
        <h1>Account Info</h1>
        <h1>{userId}</h1>
        </div>
    </>;
};

export default Account